import 'package:flutter/material.dart';

class StateMgtPage extends StatelessWidget {
  const StateMgtPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}